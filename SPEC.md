# SPEC: 리포트에 "다음 장" 희망 프레임 추가

## 배경 (조사 결과)

핵심 메커니즘은 이미 있다. [lib/reportPrompts.ts](lib/reportPrompts.ts)의 `describeUpcomingPeriod()`가 그 사람의 실제 `decadeFortune`에서 "아직 오지 않은 다음 대운"을 코드로 골라 정확한 나이·간지를 데이터 문장으로 만들고, 모델에게 "이 숫자만 쓰고 지어내지 말라"고 강제한다. 심층 리포트의 `upcoming_period_heading`/`upcoming_period_body`가 이미 이 데이터를 쓴다.

세 가지가 원하는 것과 다르다:
1. 이 두 필드는 [lib/reportLock.ts](lib/reportLock.ts)의 `LOCKED_KEYS`라 구매자만 본다.
2. 톤이 "무엇이 온다/무엇을 준비하라"이지 희망 프레임이 아니다.
3. 신년 리포트([lib/yearReportPrompts.ts](lib/yearReportPrompts.ts))에는 이 데이터 자체가 안 들어간다 — `YearReportContext`에 생년월일도 `decadeFortune`도 없다.

## 목표

유료 심층 리포트의 "다가오는 시기" 문구와 신년 리포트의 마무리에, 실제 계산된 대운 전환 시점을 근거로 은은한 "다음 장" 프레임을 넣는다. 구매 전 리포트 화면에도 그 시점을 알리는 개인화된 한 줄을 추가한다.

**핵심(사용자 2026-09-22 피드백, 2차 수정)**: 실제 사주 상담에서 힘을 갖는 건 "언젠가 좋아진다"가 아니라 "정확히 몇 살부터"라는 구체적 나이다. 사주를 보러 오는 사람 대부분은 지금이 안 좋아서 온다 — 그래서 "현재 이후의 나이"를 흐리지 않고 또렷하게 박아 넣는 것이 이 작업의 핵심이다. 나이는 항상 명확한 숫자로.

**결과 단정 수위(2차 결정)**: 애초 "결과는 단정하지 않는다"는 방향을 사용자가 수정했다 — 결과를 단정하는 것 자체가 신뢰를 높인다는 판단. 다만 전면 단정("잘될 겁니다")은 (a) 검증 불가능한 삶의 결과 약속이라 스토어/소비자보호·환불 분쟁 리스크가 있고 (b) 데이터에 안 묶여 있어서 11개 리포트가 똑같아지는 원인이 된다. 그래서 다음 2단 구조로 확정:
1. **기운 전환은 항상 확정적으로 쓴다** — "N세부터 [원소] 기운이 강해집니다"는 실제 계산값이라 단정해도 안전.
2. **그 모듈이 다루는 구체적 영역에 한해 결과도 확정적으로 쓴다** — 예: 번아웃(3번) 모듈이면 "이 시기부터 회복됩니다", 분노(6번) 모듈이면 "치받는 느낌이 가라앉습니다", 돈(2번) 모듈이면 "여유가 생깁니다". **그 모듈의 구체적 영역을 벗어난 보편적 약속("인생이 다 잘 풀립니다", "다 잘될 겁니다")은 쓰지 않는다** — 이게 리스크도 가장 크고 반복 문제의 근원이기 때문. `mindset_guide` 필드에 이미 있는 "범용적 은유 금지" 가드와 같은 패턴을 `closing_body`(와 신년 리포트 `closing`)에도 적용해서, 이 모듈/이 사람 전용 문장인지 기계적으로 강제한다.

## 포함 범위

1. **심층 리포트 톤 강화** — [lib/reportPrompts.ts](lib/reportPrompts.ts)의 `describeUpcomingPeriod()` 데이터 문구와 `buildOutputSchema()`의 `upcoming_period_heading`/`upcoming_period_body` 지시문을 "다음 장/국면 전환" 프레임으로 다시 쓴다.
2. **구매 전 미리보기 한 줄** — [mobile/screens/ReportScreen.tsx](mobile/screens/ReportScreen.tsx)는 이미 `decadeFortune`+`currentAge`를 props로 받는다. 이 값으로 AI 호출 없이 결정론적으로("32세부터, 새로운 기운이 시작돼요" 같은) 개인화 한 줄을 만들어, 잠긴 "다가오는 시기" 페이지의 고정 문구(`upcomingPeriodNote`) 옆에 보여준다.
3. **신년 리포트 반영** — `app/api/yearReport/route.ts`의 요청 바디와 `YearReportContext`에 `currentAge`/`decadeFortune`을 추가하고, 기존 `closing` 필드의 지시문에 같은 톤을 반영한다.

## 제외 범위

- 오늘의 운세·홈 화면(무료 상시 화면)에는 추가하지 않는다.
- "2~10년 사이" 같은 획일적 문구는 만들지 않는다 — 실제 계산값만 쓴다.
- "문제가 풀린다/해결된다"처럼 결과를 단정하는 표현은 쓰지 않는다.
- 대운 전환 알림(`decadeNotification`) 기능 자체는 건드리지 않는다.
- `reportLock.ts`의 `LOCKED_KEYS` 구조는 바꾸지 않는다 — `upcoming_period_heading`/`body`는 계속 잠금, 미리보기는 별도 필드.
- 신년 리포트에 새 섹션/페이지를 만들지 않는다 — 기존 `closing` 안에서 처리.

## 제약

- 정신건강에 닿는 모듈(번아웃/수면/분노 등)의 확정 결과 문장은 "회복됩니다"처럼 임상적으로 들리는 표현을 피하고 "무겁던 느낌이 가벼워집니다" 같은 체감 표현을 쓴다 — `PRODUCT.md`의 "임상 진단 도구가 아니다" 원칙과 위기 대응 프로토콜은 이번 작업으로 흔들리지 않는다.

- `CLAUDE.md` 압박 규칙: 무엇이 오는지 이름만 대고, 이유/할 일은 Pro에 잠그는 기존 패턴 유지. 가짜 긴급성, 부정 단정 금지.
- `PRODUCT.md` "Calculated, not invented": 나이·간지 숫자는 반드시 실제 계산값. 모델이 새 숫자를 지어내지 못하게 막는 기존 가드(정보 없음/범위 초과 폴백 문구)는 그대로 유지.
- 미리보기 한 줄은 서버 호출·AI 생성 없이 클라이언트에서 결정론적으로 계산한다(`mobile/lib/decadeTransition.ts`와 같은 패턴).
- ko/en/es 3개 언어 동시 처리.
- 신규 네이티브 의존성 없음 — 전부 JS/프롬프트 텍스트 변경이라 OTA로 나간다.
- 화면에 보이는 텍스트가 바뀌므로 `CLAUDE.md`의 Design skills(`impeccable`)와 "Verifying UI" 절차 대상.

## 가정

- "은은한 재구성" = "지금이 전부가 아니다"라는 문장을 직접 쓰지 않고, 다가오는 시기를 담담한 다음 장으로만 제시하는 것.
- 신년 리포트에 필요한 `currentAge`/`decadeFortune`은 앱이 이미 로컬에 가진 사주 결과에서 가져와 요청 바디에 실어 보낸다(새 계산 없음).
- 리포트 화면 미리보기는 이미 화면에 전달되는 값만 쓴다 — 서버 응답 필드 추가 없음.

## 완료 기준

- 심층 리포트: 생성된 `upcoming_period_body`가 실제 계산된 나이·간지를 **문장 앞쪽에서 명확한 숫자로** 쓴다. "머지않아"/"언젠가"류로 후퇴한 표현이 없다.
- 심층 리포트 `closing_body`(11개 모듈 각각): 기운 전환(나이+원소)은 확정 문장, 그 모듈의 구체적 영역에 한정된 결과도 확정 문장. 모듈 영역을 벗어난 보편적 "잘될 겁니다"류 문장이 없다. 서로 다른 모듈 2~3개로 생성해 비교했을 때 결과 문장이 그 모듈 주제에 맞게 실제로 달라야 한다(복붙해도 말이 되면 실패).
- 리포트 화면: 구매 전 잠긴 "다가오는 시기" 페이지에 실제 나이가 숫자로 또렷하게 박힌 개인화 한 줄이 ko/en/es 모두에서 보인다.
- 신년 리포트: `closing` 문구에 같은 규칙(나이 명확, 기운 전환 확정, 다루는 영역 한정 결과만 확정)이 실제 계산된 나이를 포함해 들어간다.
- 세 곳 모두 `tsc` 통과, 기존 회귀(`validate:manseryeok` 등) 영향 없음.

## 발견 사항

(TODO 진행 중 발견한 범위 밖 이슈를 여기 적는다.)
