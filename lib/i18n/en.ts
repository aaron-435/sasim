/**
 * lib/i18n/en.ts
 * ------------------------------------------------------------------
 * PLACEHOLDER — not translated yet (see lib/i18n/README.md). Re-exports
 * ko verbatim so the app keeps working with English selected while no
 * real English copy exists. When it's time to translate: replace the
 * re-export below with a literal object of the same shape as ko.ts,
 * with every value translated to English. TypeScript (via
 * lib/i18n/index.ts's `Dictionary` type) will flag anything missing.
 * ------------------------------------------------------------------
 */

import { ko } from "./ko";

export const en = ko;
